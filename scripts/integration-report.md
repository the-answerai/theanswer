# Integration Analysis Report

Generated: 2025-11-25T05:11:40.837Z

## Summary

- **Total Integrations:** 103
- **With Components:** 86
- **Without Components:** 17

## Integrations with Components

### Zep Memory API

**Credential:** `zepMemoryApi`

**Vector Stores:**
- Zep Collection - Cloud (`zepCloud`)
- Zep Collection - Open Source (`zep`)

**Memory:**
- Zep Memory - Cloud (`ZepMemoryCloud`)
- Zep Memory - Open Source (`ZepMemory`)

### Youtube API

**Credential:** `youtubeApi`

**MCP Servers:**
- Youtube MCP (`youtubeMCP`)

### Xai API

**Credential:** `xaiApi`

**Chat Models:**
- ChatXAI (`chatXAI`)

### WolframAlpha App ID

**Credential:** `wolframAlphaAppId`

**Tools:**
- WolframAlpha (`wolframAlpha`)

### Weaviate API

**Credential:** `weaviateApi`

**Vector Stores:**
- Weaviate (`weaviate`)

### Voyage AI API

**Credential:** `voyageAIApi`

**Embeddings:**
- VoyageAI Embeddings (`voyageAIEmbeddings`)

**Retrievers:**
- Voyage AI Rerank Retriever (`voyageAIRerankRetriever`)

### Vectara API

**Credential:** `vectaraApi`

**Vector Stores:**
- Vectara (`vectara`)

### Upstash Vector API

**Credential:** `upstashVectorApi`

**Vector Stores:**
- Upstash Vector (`upstash`)

### Upstash Redis Memory API

**Credential:** `upstashRedisMemoryApi`

**Memory:**
- Upstash Redis-Backed Chat Memory (`upstashRedisBackedChatMemory`)

### Unstructured API

**Credential:** `unstructuredApi`

**Document Loaders:**
- Unstructured Folder Loader (`unstructuredFolderLoader`)
- Unstructured File Loader (`unstructuredFileLoader`)

### TogetherAI API

**Credential:** `togetherAIApi`

**Chat Models:**
- ChatTogetherAI (`chatTogetherAI_LlamaIndex`)
- ChatTogetherAI (`chatTogetherAI`)

**LLMs:**
- TogetherAI (`togetherAI`)

**Embeddings:**
- TogetherAIEmbedding (`togetherAIEmbedding`)

### Tavily API

**Credential:** `tavilyApi`

**Tools:**
- Tavily API (`tavilyAPI`)

### Supabase API

**Credential:** `supabaseApi`

**Vector Stores:**
- Supabase (`supabase`)

### Stripe API

**Credential:** `stripeApi`

**Tools:**
- StripeAgentTool (`stripeAgentTool`)

### Spider API

**Credential:** `spiderApi`

**Document Loaders:**
- Spider Document Loaders (`spiderDocumentLoaders`)

### Slack API

**Credential:** `slackApi`

**MCP Servers:**
- Slack MCP (`slackMCP`)

### SingleStore API

**Credential:** `singleStoreApi`

**Vector Stores:**
- SingleStore (`singlestore`)

### Serper API

**Credential:** `serperApi`

**Tools:**
- Serper (`serper`)

### Serp API

**Credential:** `serpApi`

**Document Loaders:**
- SerpApi For Web Search (`serpApi`)

**Tools:**
- Serp API (`serpAPI`)

### Search API

**Credential:** `searchApi`

**Document Loaders:**
- SearchApi For Web Search (`searchApi`)

**Tools:**
- SearchApi (`searchAPI`)

### Salesforce OAuth

**Credential:** `salesforceOAuth`

**MCP Servers:**
- Salesforce OAuth MCP (`salesforceOauthMcp`)

### Salesforce API

**Credential:** `salesforceApi`

**MCP Servers:**
- Salesforce MCP (`sfdcMCP`)

### Replicate API

**Credential:** `replicateApi`

**LLMs:**
- Replicate (`replicate`)

### Redis URL

**Credential:** `redisCacheUrlApi`

**Vector Stores:**
- Redis (`redis`)

**Memory:**
- Redis-Backed Chat Memory (`RedisBackedChatMemory`)

### Redis API

**Credential:** `redisCacheApi`

**Vector Stores:**
- Redis (`redis`)

**Memory:**
- Redis-Backed Chat Memory (`RedisBackedChatMemory`)

### Qdrant API

**Credential:** `qdrantApi`

**Vector Stores:**
- Qdrant (`qdrant`)

### Postgres URL

**Credential:** `PostgresUrl`

**MCP Servers:**
- PostgreSQL MCP (`postgreSQLMCP`)

### Postgres API

**Credential:** `PostgresApi`

**Vector Stores:**
- Postgres (`postgres`)
- AAI Vector (Postgres) (`aaiPostgres`)

**Memory:**
- Agent Memory (`agentMemory`)
- Postgres Agent Memory (`postgresAgentMemory`)

### Pinecone API

**Credential:** `pineconeApi`

**Vector Stores:**
- Pinecone (`pineconeLlamaIndex`)
- Pinecone (`pinecone`)

### Perplexity API

**Credential:** `perplexityApi`

**Chat Models:**
- ChatPerplexity (`chatPerplexity`)

### OpenSearch

**Credential:** `openSearchUrl`

**Vector Stores:**
- OpenSearch (`openSearch`)

### OpenRouter API Key

**Credential:** `openRouterApi`

**Chat Models:**
- ChatOpenRouter (`chatOpenRouter`)

### OpenAI API

**Credential:** `openAIApi`

**Chat Models:**
- ChatOpenAI Custom (`chatOpenAICustom`)
- ChatOpenAI (`chatOpenAI_LlamaIndex`)
- ChatOpenAI (`chatOpenAI`)

**LLMs:**
- OpenAI (`openAI`)

**Embeddings:**
- OpenAI Embedding (`openAIEmbedding_LlamaIndex`)
- OpenAI Embeddings (`openAIEmbeddings`)
- OpenAI Embeddings Custom (`openAIEmbeddingsCustom`)

**Tools:**
- Dall-E Post (`dallePost`)

### Google OAuth

**Credential:** `googleOAuth`

**Document Loaders:**
- Google Drive (`googleDrive`)
- Gmail (`gmail`)

**Tools:**
- Update Google Calendar Event (`updateGoogleCalendar`)
- Retrieve Google Calendar Events (`retrieveGoogleCalendar`)
- Delete Google Calendar Event (`deleteGoogleCalendar`)
- Create Google Calendar Event (`createGoogleCalendar`)

### NVIDIA NGC API Key

**Credential:** `nvidiaNIMApi`

**Chat Models:**
- Chat NVIDIA NIM (`Chat NVIDIA NIM`)

### Notion API

**Credential:** `notionApi`

**Document Loaders:**
- Notion Page (`notionPage`)
- Notion Database (`notionDB`)

### N8n API

**Credential:** `n8nApi`

**Tools:**
- N8n (`n8n`)

### MySQL API

**Credential:** `MySQLApi`

**Memory:**
- Agent Memory (`agentMemory`)
- MySQL Agent Memory (`mySQLAgentMemory`)

### MongoDB ATLAS

**Credential:** `mongoDBUrlApi`

**Vector Stores:**
- MongoDB Atlas (`mongoDBAtlas`)

**Memory:**
- MongoDB Atlas Chat Memory (`MongoDBAtlasChatMemory`)

### MistralAI API

**Credential:** `mistralAIApi`

**Chat Models:**
- ChatMistral (`chatMistral_LlamaIndex`)
- ChatMistralAI (`chatMistralAI`)

**Embeddings:**
- MistralAI Embeddings (`mistralAIEmbeddings`)

### Milvus Auth

**Credential:** `milvusAuth`

**Vector Stores:**
- Milvus (`milvus`)

### Mem0 Memory API

**Credential:** `mem0MemoryApi`

**Memory:**
- Mem0 (`mem0`)

### Meilisearch API

**Credential:** `meilisearchApi`

**Vector Stores:**
- Meilisearch (`meilisearch`)

### Make.Com API

**Credential:** `makeComApi`

**Tools:**
- MakeComWebhook (`makeComWebhook`)

### LocalAI API

**Credential:** `localAIApi`

**Chat Models:**
- ChatLocalAI (`chatLocalAI`)

**Embeddings:**
- LocalAI Embeddings (`localAIEmbeddings`)

### Litellm API

**Credential:** `litellmApi`

**Chat Models:**
- ChatLitellm (`chatLitellm`)

### Linear API

**Credential:** `linearApi`

**MCP Servers:**
- Linear MCP (`linearMCP`)

### Jira API

**Credential:** `JiraApi`

**MCP Servers:**
- Jira MCP (`jiraMCP`)

### JinaAI API

**Credential:** `jinaAIApi`

**Embeddings:**
- Jina Embeddings (`jinaEmbeddings`)

**Retrievers:**
- Jina AI Rerank Retriever (`JinaRerankRetriever`)

### IBM Watsonx

**Credential:** `ibmWatsonx`

**Chat Models:**
- ChatIBMWatsonx (`chatIBMWatsonx`)

**LLMs:**
- IBMWatsonx (`ibmWatsonx`)

**Embeddings:**
- IBM Watsonx Embeddings (`ibmEmbedding`)

### HuggingFace API

**Credential:** `huggingFaceApi`

**Chat Models:**
- ChatHuggingFace (`chatHuggingFace`)

**LLMs:**
- HuggingFace Inference (`huggingFaceInference_LLMs`)

**Embeddings:**
- HuggingFace Inference Embeddings (`huggingFaceInferenceEmbeddings`)

### Groq API

**Credential:** `groqApi`

**Chat Models:**
- GroqChat (`groqChat`)
- ChatGroq (`chatGroq_LlamaIndex`)

### Google Custom Search API

**Credential:** `googleCustomSearchApi`

**Tools:**
- Google Custom Search (`googleCustomSearch`)

### Google Generative AI

**Credential:** `googleGenerativeAI`

**Chat Models:**
- ChatGoogleGenerativeAI (`chatGoogleGenerativeAI`)

**Embeddings:**
- GoogleGenerativeAI Embeddings (`googleGenerativeAiEmbeddings`)

### Google Vertex Auth

**Credential:** `googleVertexAuth`

**Chat Models:**
- ChatGoogleVertexAI (`chatGoogleVertexAI`)

**LLMs:**
- GoogleVertexAI (`googlevertexai`)

**Embeddings:**
- GoogleVertexAI Embeddings (`googlevertexaiEmbeddings`)

### Github API

**Credential:** `githubApi`

**Document Loaders:**
- Github (`github`)

**MCP Servers:**
- Github MCP (`githubMCP`)

### Gamma API

**Credential:** `gammaApi`

**Tools:**
- Gamma (`gamma`)

### Fireworks API

**Credential:** `fireworksApi`

**Chat Models:**
- ChatFireworks (`chatFireworks`)

**LLMs:**
- Fireworks (`fireworks`)

### FireCrawl API

**Credential:** `fireCrawlApi`

**Document Loaders:**
- FireCrawl (`fireCrawl`)

### Figma API

**Credential:** `figmaApi`

**Document Loaders:**
- Figma (`figma`)

### Exa Search API

**Credential:** `exaSearchApi`

**Tools:**
- Exa Search (`exaSearch`)

### ElasticSearch User Password

**Credential:** `elasticSearchUserPassword`

**Vector Stores:**
- Elasticsearch (`elasticsearch`)

### Elasticsearch API

**Credential:** `elasticsearchApi`

**Vector Stores:**
- Elasticsearch (`elasticsearch`)

### E2B API

**Credential:** `E2BApi`

**Tools:**
- Code Interpreter by E2B (`codeInterpreterE2B`)

### DynamodbMemory API

**Credential:** `dynamodbMemoryApi`

**Memory:**
- DynamoDB Chat Memory (`DynamoDBChatMemory`)

### DeepseekAI API

**Credential:** `deepseekApi`

**Chat Models:**
- ChatDeepseek (`chatDeepseek`)

### Couchbase API

**Credential:** `couchbaseApi`

**Vector Stores:**
- Couchbase (`couchbase`)

### Contentful Management API

**Credential:** `contentfulManagementApi`

**MCP Servers:**
- Contentful MCP (`contentfulMCP`)

### Contentful Delivery API

**Credential:** `contentfulDeliveryApi`

**Document Loaders:**
- Contentful (`contentful`)

### Confluence Server/Data Center API

**Credential:** `confluenceServerDCApi`

**Document Loaders:**
- Confluence (`confluence`)

### Confluence Cloud API

**Credential:** `confluenceCloudApi`

**Document Loaders:**
- Confluence (`confluence`)

**MCP Servers:**
- Confluence MCP (`confluenceMCP`)

### Composio API

**Credential:** `composioApi`

**Tools:**
- Composio (`composio`)

### Cohere API

**Credential:** `cohereApi`

**Chat Models:**
- ChatCohere (`chatCohere`)

**LLMs:**
- Cohere (`cohere`)

**Embeddings:**
- Cohere Embeddings (`cohereEmbeddings`)

**Retrievers:**
- Cohere Rerank Retriever (`cohereRerankRetriever`)

### Chroma API

**Credential:** `chromaApi`

**Vector Stores:**
- Chroma (`chroma`)

### Chatflow API

**Credential:** `chatflowApi`

**Tools:**
- Chatflow Tool (`ChatflowTool`)

### Cerebras API Key

**Credential:** `cerebrasAIApi`

**Chat Models:**
- ChatCerebras (`chatCerebras`)

### Brave Search API

**Credential:** `braveSearchApi`

**Document Loaders:**
- BraveSearch API Document Loader (`braveSearchApiLoader`)

**Tools:**
- BraveSearch API (`braveSearchAPI`)

**MCP Servers:**
- Brave Search MCP (`braveSearchMCP`)

### Baidu Qianfan API

**Credential:** `baiduQianfanApi`

**Chat Models:**
- ChatBaiduWenxin (`chatBaiduWenxin`)

### Azure OpenAI API

**Credential:** `azureOpenAIApi`

**Chat Models:**
- AzureChatOpenAI (`azureChatOpenAI_LlamaIndex`)
- Azure ChatOpenAI (`azureChatOpenAI`)

**LLMs:**
- Azure OpenAI (`azureOpenAI`)

**Embeddings:**
- Azure OpenAI Embeddings (`azureOpenAIEmbeddingsLlamaIndex`)
- Azure OpenAI Embeddings (`azureOpenAIEmbeddings`)

### Atlassian OAuth

**Credential:** `atlassianOAuth`

**MCP Servers:**
- Atlassian MCP (`atlassianMcp`)

### Astra DB API

**Credential:** `AstraDBApi`

**Vector Stores:**
- Astra (`Astra`)

### Apify API

**Credential:** `apifyApi`

**Document Loaders:**
- Apify Website Content Crawler (`apifyWebsiteContentCrawler`)

### Anthropic API

**Credential:** `anthropicApi`

**Chat Models:**
- ChatAnthropic (`chatAnthropic_LlamaIndex`)
- ChatAnthropic (`chatAnthropic`)

### Alibaba API

**Credential:** `AlibabaApi`

**Chat Models:**
- ChatAlibabaTongyi (`chatAlibabaTongyi`)

### Airtable API

**Credential:** `airtableApi`

**Document Loaders:**
- Airtable (`airtable`)

**Agents:**
- Airtable Agent (`airtableAgent`)

### AWS security credentials

**Credential:** `awsApi`

**Chat Models:**
- AWS ChatBedrock (`awsChatBedrock`)

**LLMs:**
- AWS Bedrock (`awsBedrock`)

**Embeddings:**
- AWS Bedrock Embeddings (`AWSBedrockEmbeddings`)

**Document Loaders:**
- S3 (`S3`)
- S3 Directory (`s3Directory`)

**Retrievers:**
- AWS Bedrock Knowledge Base Retriever (`awsBedrockKBRetriever`)

## ⚠️ Orphaned Credentials

These credentials exist but have NO associated components (no loaders, tools, models, etc.):

| Credential Name | Label | File Path |
|-----------------|-------|----------|
| `upstashRedisApi` | Upstash Redis API | `packages/components/credentials/UpstashRedisApi.credential.ts` |
| `phoenixApi` | Phoenix API | `packages/components/credentials/PhoenixApi.credential.ts` |
| `opikApi` | Opik API | `packages/components/credentials/OpikApi.credential.ts` |
| `neo4jApi` | Neo4j API | `packages/components/credentials/Neo4jApi.credential.ts` |
| `momentoCacheApi` | Momento Cache API | `packages/components/credentials/MomentoCacheApi.credential.ts` |
| `lunaryApi` | Lunary AI | `packages/components/credentials/LunaryApi.credential.ts` |
| `langsmithApi` | Langsmith API | `packages/components/credentials/LangsmithApi.credential.ts` |
| `langfuseApi` | Langfuse API | `packages/components/credentials/LangfuseApi.credential.ts` |
| `langwatchApi` | LangWatch API | `packages/components/credentials/LangWatchApi.credential.ts` |
| `jlincApi` | JLINC API | `packages/components/credentials/JlincApi.credential.ts` |
| `httpBearerToken` | HTTP Bearer Token | `packages/components/credentials/HTTPBearerToken.credential.ts` |
| `httpBasicAuth` | HTTP Basic Auth | `packages/components/credentials/HTTPBasicAuth.credential.ts` |
| `httpApiKey` | HTTP Api Key | `packages/components/credentials/HTTPApiKey.credential.ts` |
| `googleMakerSuite` | Google MakerSuite | `packages/components/credentials/GoogleMakerSuite.credential.ts` |
| `azureCognitiveServices` | Azure Cognitive Services | `packages/components/credentials/AzureCognitiveServices.credential.ts` |
| `assemblyAIApi` | AssemblyAI API | `packages/components/credentials/AssemblyAI.credential.ts` |
| `arizeApi` | Arize API | `packages/components/credentials/ArizeApi.credential.ts` |

**Note:** These credentials may be used for custom implementations or are legacy credentials that are no longer in use.
